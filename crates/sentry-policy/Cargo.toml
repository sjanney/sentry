[package]
name = "sentry-policy"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
sentry-types = { path = "../sentry-types" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
